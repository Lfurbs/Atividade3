#include<stdio.h>

main(){
     
    //operadores relacionais = <> >= <= 

    int a = 0, b = 18;

    printf("Informe sua idade:");
    scanf("%d", &a);

    if ( a >= b)
    {
        printf("Voce e de maior");
    }else
    {
       printf("Voce e de menor");
    }
}
